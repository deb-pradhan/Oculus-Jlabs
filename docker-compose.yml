services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: oculus
      POSTGRES_USER: oculus
      POSTGRES_PASSWORD: oculus
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
